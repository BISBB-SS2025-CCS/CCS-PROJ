# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Dependency directories
# These should almost universally be ignored.
# They are installed by package managers (npm, yarn, pnpm) based on package.json and package-lock.json.
node_modules/
jspm_packages/

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Optional TypeScript Build cache
*.tsbuildinfo

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variable files
# Contains sensitive credentials and environment-specific configurations.
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test
!.env.example
!.env.sample

# Build output/distributable files
# These are generated files and should not be versioned.
dist/
build/
out/
public/ # For some frameworks like Next.js (static export) or Gatsby
.next/ # Next.js build output
.nuxt/ # Nuxt.js build output
.svelte-kit/ # SvelteKit build output
.vercel/ # Vercel deployment output

# Coverage directory
coverage/
*.lcov
.nyc_output/

# IDE files and directories
# Add these if you don't have a global gitignore for them.
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Operating System files
.DS_Store # macOS
Thumbs.db # Windows
._* # macOS resource forks
.Spotlight-V100 # macOS
.Trashes # macOS

# Temporary files
*.tmp
*.bak
*.swp

# PM2 files
pm2.log
*.pm2.pid
*.pm2.log
ecosystem.config.js # If it contains sensitive info or machine-specific paths; otherwise, consider versioning.

# Specific package manager lock files (beyond package-lock.json, yarn.lock, pnpm-lock.yaml which SHOULD be committed)
# For Yarn PnP
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# WebStorm
.idea/
*.iml

# Other common ignores
# For example, if using a local SQLite database for development
*.sqlite
*.sqlite3